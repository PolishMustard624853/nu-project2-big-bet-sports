<!-- Loop over each object in the leagues array -->
{{#each leagues as |league| }}
<div class="row mb-4 league">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/league/{{league.id}}">{{league.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount league.needed_funding}}</span>
      needed!
    </p>
  </div>
  <div class="col-md-7">
    <p>
      {{league.description}}
    </p>
  </div>
</div>
{{/each}}
